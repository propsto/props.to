{
  "name": "@propsto/app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm dotenv -- next build",
    "dev": "pnpm dotenv -- pnpm next-dev",
    "dotenv": "dotenv -e ../../.env",
    "dx": "pnpm --filter @propsto/data db-setup && pnpm dev",
    "dx:nuke": "pnpm --filter @propsto/data db-nuke && pnpm dx",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "postinstall": "pnpm --filter @propsto/data db-generate",
    "lint": "next lint",
    "next-dev": "next dev --hostname=$PROPSTO_APP_HOSTNAME --port=$PROPSTO_APP_PORT",
    "next-start": "next start --hostname=$PROPSTO_APP_HOSTNAME --port=$PROPSTO_APP_PORT",
    "start": "pnpm dotenv -- pnpm next-start",
    "test": "pnpm test:unit && pnpm test:e2e",
    "test:e2e": "playwright test --pass-with-no-tests",
    "test:unit": "vitest run --passWithNoTests",
    "test:unit:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "@playwright/test": "*",
    "@propsto/auth": "workspace:*",
    "@propsto/config": "workspace:*",
    "@propsto/constants": "workspace:*",
    "@propsto/data": "workspace:*",
    "@propsto/logger": "workspace:*",
    "@propsto/ui": "workspace:*",
    "@types/node": "*",
    "@types/react": "*",
    "@types/react-dom": "*",
    "autoprefixer": "*",
    "dotenv-cli": "*",
    "dotenv-expand": "*",
    "eslint": "*",
    "lucide-react": "*",
    "next": "*",
    "next-auth": "*",
    "next-themes": "*",
    "postcss": "*",
    "react": "*",
    "react-dom": "*",
    "tailwind-merge": "*",
    "tailwindcss": "*",
    "typescript": "*",
    "vitest": "*",
    "zod": "*"
  }
}